[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "gitnotes"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
]

[project.scripts]
gitnotes = "gitnotes.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
